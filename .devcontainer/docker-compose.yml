version: '3.8'

services:
  app:
    image: node:20-bullseye
    working_dir: /workspace
    volumes:
      - ./:/workspace:cached
    command: /bin/bash -lc "npm install && npm run dev"
    ports:
      - "3000:3000"
